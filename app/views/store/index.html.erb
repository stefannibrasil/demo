<% if notice %>
  <aside id="notice"><%= notice %></aside>
<% end %>
<h1>Your Pragmatic Catalog</h1>
<ul class="catalog">
  <% cache @products do %>
    <% @products.each do |product| %>
      <% cache @products do %>
        <li>
          <% if product.image.attached? %>
            <%= image_tag product.image.variant(resize: "320x320"), :size => "80x80" %>
          <% end %>
          <h2><%= product.title %></h2>
          <p>
            <%= sanitize(product.description) %>
          </p>
          <div class="price">
            <%= number_to_currency(product.price) %>
          </div>
        </li>
      <%end %>
    <% end %>
  <% end %>
</ul>
